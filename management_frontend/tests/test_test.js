'use strict';

describe('First suite', function() {
    it('should not fail ever', function() {
        expect(true).toBe(true);
    });
    // it('should fail always', function() {
    //     expect(1).toBe(5);
    // });
});
